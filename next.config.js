module.exports = {
  eslint: {
    ignoreDuringBuilds: true, // Disable ESLint checks during build
  },
  reactStrictMode: true,
  swcMinify: false, // Disable SWC minification to prevent loading issues
}
